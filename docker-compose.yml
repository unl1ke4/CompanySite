services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - media_volume:/app/media
      - static_volume:/app/staticfiles
    command: gunicorn company_site.wsgi:application --bind 0.0.0.0:8000

volumes:
  static_volume:
  media_volume: